import { _Type, ComplementPackage } from '.';
export class CatPackage extends _Type {
    product_id?: number;
    product_count?: number;
    pill_id?: number;
    pill_count?: number;
    complements?: ComplementPackage[];
  }

