import { HasFeatureDirective } from './has-feature.directive';

describe('HasFeatureDirective', () => {
  it('should create an instance', () => {
    const directive = new HasFeatureDirective();
    expect(directive).toBeTruthy();
  });
});
