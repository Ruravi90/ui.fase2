# Task List: Serene Login Design

- [x] Update `src/index.html` with Google Fonts (`Cormorant Garamond` and `Outfit`).
- [x] Add global CSS variables and base styles to `src/styles.scss`.
- [x] Refactor `src/app/views/login/login.component.html` to remove generic Bootstrap classes and set up the split-screen layout.
- [x] Create and implement `src/app/views/login/login.component.scss` with the custom styling and organic background.
- [x] Verify the design looks correct and the angular dev server handles the styling without errors.
